export interface FavNews{
    favouriteNews : any;
    loading : boolean;
    error : string;
}

export interface FavEvents{
    favouriteEvents : any;
    loading : boolean;
    error : string;
}

export interface FavWorkspace{
    favouriteWorkspaces : any;
    loading : boolean;
    error : string;
}

export interface FavDocuments{
    favouriteDocuments : any;
    loading : boolean;
    error : string;
}